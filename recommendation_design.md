# 探索页推荐 · 设计方案 (v1)

目标三条: **进页不卡**、**未登录也有内容**、**算一次缓存住, 固定周期在空闲时重算**。

现状问题: 推荐是"翻页时现发请求"——每页拿几个种子, 每个种子一个 `/p1/subjects/{id}/recs`,
一次会话实测 946 个请求, 且全部发生在用户正在看这一页的时候。

---

## 一、整体结构: 三层, 网络只在中间那层

```
L1 兴趣画像   本地 Room, 零请求   ──┐
                                    ├─► L2 候选召回  网络, 预算 ≤12 请求, 后台跑
L3 排序+多样性+分组  本地, 零请求  ◄─┘
                          │
                          ▼
                   Room: recommendation_feed  ──► 进页直接画 (零请求)
```

**进探索页永远只做一件事: 从 Room 读 feed 画出来。** 网络重算是另一条线, 结果下次进页才生效
(或用户点「换一批」时立刻生效)。这是"不卡"的全部秘密。

---

## 二、L1 兴趣画像 (本地, 零请求)

### 作品权重 w

按收藏状态与自评打分, 再乘时间衰减:

| 状态 | w |
|---|---|
| 看过 + 自评 ≥ 8 | +3.0 |
| 看过 + 自评 6–7 | +1.0 |
| 看过 + 未评分 | +1.5 |
| 看过 + 自评 ≤ 5 | −2.0 |
| 在看 + 进度 ≥ 30% | +2.5 |
| 在看 + 进度 < 30% | +0.8 |
| 想看 | +0.6 |
| 搁置 | −0.5 |
| 抛弃 | −2.0 |

时间衰减 `w * exp(-ln2 * Δdays / 180)`, `Δdays` 取 `lastUpdated`。进度从 `episode_collection`
本地算, 不发请求。

### 标签降噪 (四步, 全部本地)

1. **只留有区分度的 kind**: 用现成的 `CanonicalTagKind`, 保留 `Genre / Emotion / Setting /
   Audience / Source`, 丢掉 `Region`(日本)、`Category`(TV/剧场版)、`Rating`、`Technology`。
   `Series`(高达/Fate) 不进画像, 但**留给多样性约束用**。
   —— 这一步直接解决"动画/日本"过宽, 而且词表是现成的。
2. **条目内可信度**: `tag.count / 该条目最大 tag.count < 0.15` 的丢掉 (极少数人打的标签)。
3. **全库区分度 (IDF)**: `idf = ln(N / df)`, `df` = 该标签在用户收藏里出现的条目数。
   很多作品都有的标签自动降权。
4. **累加取头部**: `score(tag) = Σ (w * 可信度 * idf)`, 归一化后取 **top 3–6**。

产物很小 (几百字节), 存 **DataStore**:

```kotlin
data class InterestProfile(
    val tags: List<WeightedTag>,     // 3–6 个方向
    val seeds: List<Int>,            // 权重最高的 1–3 部, 给"因为你喜欢"用
    val computedAt: Long,
    val hash: String,                // 收藏变动检测: 变了才需要重新召回
)
```

---

## 三、L2 候选召回 (网络, 硬预算)

**每组一次请求, 总预算 ≤ 12。** 不再"每个标签抽 10 个", 而是各组产出汇进**一个候选池**,
命中多个高权重兴趣的作品分数叠加而不是重复出现。

| 组 | 请求 | 说明 |
|---|---|---|
| 高分口味 | `POST /v0/search/subjects` `tag=[top1]` + `rank>=1` + sort=RANK | top1..top3 各一次, 合并进池 (**不是各成一行**) |
| 因为你喜欢《X》 | `/p1/subjects/{id}/recs` × **1–3 个种子** | 现在是每翻一页发一批, 这是 946 的来源 |
| 本季 | 日历 / `airDate >= 本季初` + sort=HEAT | 1 |
| 换换口味 | 取 top1 **同 kind 的邻居标签** (喜欢"治愈"→"温情/纯爱") | 1 |
| 大家在看 | `/p1/trending/subjects` (已有) | 1 |

**`ranks` 的坑**: 服务端把"无排名"记作 rank **0**, 所以要"有排名"只能写 `">=1"`, 写 `"<=500"`
会把一堆没排名的垃圾排最前。

`filter.tag` 是 **AND 语义**, 所以多标签只能分次请求再合并, 不能指望一次 OR。

---

## 四、L3 排序 / 稳定随机 / 多样性 (本地)

**综合分**: `0.45×标签匹配 + 0.20×种子相似 + 0.15×bangumi 排名与评分人数 + 0.10×新鲜度 + 0.10×探索价值`

**加权随机, 不是等概率**: 按分数做加权抽样, 高相关更容易出现但中后部仍有机会。

**稳定随机**: 抽样的随机源用 `seed = hash(userId, epochDay, groupKey, nonce)` ——
同一天进来结果稳定, 第二天自动换一部分; 点「换一批」时 `nonce++` 立刻换。

**多样性约束** (全部本地可判):
- 同系列只留一部 —— 用 `SubjectCollectionEntity.relations.seriesMainSubjectIds`, 现成的。
- 同主标签连续不超过 2 部。
- 已收藏 / 抛弃的全排除 (Room 一次查询)。
- 「想看」不混进新推荐, 单独成区。
- 曝光多次未点击的逐步降权 (feed 表加一列 `impressions`)。

---

## 五、缓存与"空闲时重算"

新增 Room 表 (v24 → 25, AutoMigration):

```kotlin
@Entity("recommendation_feed")
data class RecommendationFeedEntity(
    @PrimaryKey val groupKey: String,   // "for_you_high_rated" / "because_you_liked" / ...
    val titleArgs: String,              // 《X》之类的填充参数, 标题文案仍走 strings.xml
    val subjectsJson: String,           // 条目快照: id + 名字 + 封面 (进页直接画, 不必再查条目)
    val computedAt: Long,
    val profileHash: String,
    val impressions: Int = 0,
)
```

存快照而不是只存 id: 进页要能**立刻画**, 再去查条目就又回到"进页发请求"了。

### 时序

1. 进页 → 读 Room → 立刻画 (与「继续观看」同样快)
2. 同时判新鲜度: `computedAt` 超 TTL **或** `profileHash` 变了 → 标记需要重算
   - 个性化组 TTL 12h; 本季/trending 组 TTL 3h
3. **重算不在进页那一刻做**, 等空闲信号 (三个都满足):
   - 首帧已上屏 (探索页 `LaunchedEffect` 里 delay ~1.5s)
   - 没有正在播放
   - 没有在跑缓存下载/合并 —— 合并会把整机 IO 压停, 别去抢
4. 重算完写 Room → Flow 推给 UI, 但**不在用户眼皮底下重排**: 下次进页才换, 或点「换一批」立刻换

### 落点必须注意

重算要跑在 **Repository 自己持有的 scope** 上, 不能挂调用方协程 ——
TV hero 那条 `collectLatest` 换焦点就会把它整个掐掉, 这个坑刚踩过一次
(详情页"连选集卡骨架都没有"就是它)。

---

## 六、未登录 / 冷启动

没登录 = 零收藏 = 画像为空。这时用**三组不需要登录的默认组**:

1. 大家最近在看 (trending)
2. 本季新番 (日历)
3. 高分经典 (`rank>=1` + sort=RANK)

三个请求**并发发, 谁先回谁先画**, 每组独立渲染。它们的结果同样进 Room, 所以第二次冷启动是
本地读。**不随包塞兜底 JSON** —— 外部资源应当运行时拉取并缓存。

登录但收藏 < 5 部时: 默认组打底 + 少量个性化, 随收藏增长逐步替换。

---

## 七、分组落地 (6 行)

| # | 行 | 数据来源 | 登录要求 |
|---|---|---|---|
| 1 | 继续观看 | 已有, 不动 | 是 |
| 2 | 因为你喜欢《X》 | 种子 recs, **合并成一行**, 理由取权重最高那部 | 是 |
| 3 | 符合你口味的高分动画 | top tags 合并池 | 是 |
| 4 | 本季你可能会喜欢 | 日历 + 画像重排 | 否 |
| 5 | 换换口味 | 邻居标签 | 是 |
| 6 | 大家最近在看 | trending | 否 |

只有当某个标签**非常强且候选足够**时才给它独立一行 (如"你喜欢的治愈系动画"), 否则合并进第 3 行。

### TV 端的影响

`TvExplorationPage` 现在是「继续观看 1 行 + 推荐 N 行无限流」, 行号映射 `itemIndexOfRowKey`
里有一堆补偿逻辑, 都是为了对付"分页迟到往列表头部插 item"。**改成固定 5–6 行后这些能简化**:
行数不再随分页变, hero 锚点也不会被插入的 item 顶走。

---

## 八、分步实施

- **P0** 把现有推荐从"翻页现发请求"改成"Room 缓存 + 后台重算", 分组仍只有一组。
  → 946 请求当场消失, 进页变成本地读。**这一步单独可发版。**
- **P1** L1 画像 + L2 多组召回 + 六行分组。
- **P2** 稳定随机 / 换一批 / 曝光降权 / 多样性约束。
